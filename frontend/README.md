# Frontend — Sistema de Gestión de Correspondencia

> Placeholder del frontend. Se inicializará con Vite + React + TypeScript.

## Inicializar

```bash
npx create-vite@latest ./ --template react-ts
npm install
npm run dev
```
