// Placeholder — Módulo de Autenticación (Auth)
// Estructura: router → service → repository → schema → types
import { Router } from 'express';

const router = Router();

// TODO: Implementar endpoints de autenticación
// POST /login
// POST /register
// POST /refresh-token
// POST /logout

export { router as authRouter };
